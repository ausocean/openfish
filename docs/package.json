{
  "name": "docs",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "pnpm build:openapi && astro dev",
    "build": "pnpm build:openapi && astro check && astro build",
    "preview": "astro preview",
    "build:openapi": "swag init --dir ../cmd/openfish --output ./src/assets --outputTypes json --v3.1"
  }
}