{
    "name": "@openfish/docs",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "pnpm build:openapi && vitepress dev .",
        "build": "pnpm build:openapi && vitepress build .",
        "preview": "vitepress preview .",
        "build:openapi": "swag init --dir ../cmd/openfish --output ./developer-docs/api --outputTypes json --v3.1"
      }
}